<h1>All Users</h1>
<table class="table-condensed">
<tr>
	<th>Name</th>
	<th>Email</th>
</tr>
<% @users.each do |user| %>
<tr>
	<td><%= user.name %></td>
	<td><%= user.email %></td>
</tr>
<% end %>
</table>

<%= link_to "Back to profile", user_path(current_user) %>
