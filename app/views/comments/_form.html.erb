  <%= form_for([@post, @comment], remote: true) do |f| %>
  <%= f.text_field :content, class: "form-control", placeholder: "Add a comment", id: "comment_content" %>
  <%= f.submit "Submit", class: "btn btn-danger" %>
  <% end %>